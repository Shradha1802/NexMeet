let is_Prod=true; //in casee of localhost make it false
const server=is_Prod ? "https://nexmeet-mpbb.onrender.com":"http://localhost:8080"
export default server;

//in casee of localhost make it false